// Implement Promise.all function with a function that get an array of promises and returns a promise

// const promiseArray = [
//   [promise1, promise2],
//   [promise3, promise4],
//   [promise5, promise6],
// ];

// const promise4all = Promise.all(
//   promiseArray.map(function (innerPromiseArray) {
//     return Promise.all(innerPromiseArray);
//   })
// );

// const promise4All = Promise.all(promiseArray.map(Promise.all.bind(Promise)))
